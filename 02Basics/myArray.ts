const superHeros: string[] = [];
//! const heroPower: number[] = [];
const heroPower: Array<number> = [];

superHeros.push("superman");
heroPower.push(8);

type User = {
  name: string;
  isActive: boolean;
};

const allUser: User[] = [];

allUser.push({ name: "Vaibhav", isActive: false });

const MLModels: number[][] = [[255, 255, 255], []];

export {};
